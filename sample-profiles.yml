jaffle_shop:
  target: dev_dws
  outputs:
    dev_dws:
      type: gaussdbdws
      host: ${DB_HOST}
      user: ${DB_USER}
      password: ${DB_PASS}
      port: ${DB_PORT}
      dbname: ${DB_NAME}
      schema: ${DB_SCHEMA}
      threads: 1
